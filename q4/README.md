# Question-4
Write a Python function parse_markdown(markdown) that converts Markdown to HTML according to CommonMark 0.31.2.

Requirements:
Define exactly one callable function: parse_markdown(markdown).
Use pure Python and only the standard library.
No subprocess, shell commands, network calls, or external packages.
Return HTML as a string and match expected output exactly.

Validation:
Runs quick smoke tests in Pyodide in your browser for instant feedback.
Tests your function against sample cases from the CommonMark spec: src/commonmark_spec.json.
All validation happens client-side using Pyodide—no server submission.

Helpful references:
Spec cases: src/commonmark_spec.json in the format [[markdown, html], [markdown, html], ...]
Sample implementation: test/sample_markdown_parser.py

# Solution:

## I genuinely do not understand this question.. i just copy paste the code from Chatgpt.. and it works.. maybe it will also works for you
## Just copy and Paste solution.py (attached this repo) and you are done
